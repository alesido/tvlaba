<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="@dimen/tv_program_credits_card_width"
    android:layout_height="@dimen/tv_program_credits_card_height"
    >
    <ImageView
        android:id="@+id/tvProgramCreditsCardPoster"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/tvProgramCreditsCardFooter"
        android:contentDescription="@string/content_description_tv_channel_card_poster"
        />
    <View
        android:id="@+id/tvProgramCreditsCardFooter"
        android:layout_width="match_parent"
        android:layout_height="@dimen/tv_program_credits_card_title_height"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:background="@color/lb_basic_card_bg_color"
        />
    <TextView
        android:id="@+id/tvProgramCreditsCardTitle"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="8dp"
        android:gravity="center_vertical"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/tvProgramCreditsCardPoster"
        app:layout_constraintEnd_toEndOf="parent"
        android:maxLines="2"
        android:textSize="14sp"
        android:ellipsize="end"
        tools:text="Leading actor Games Carlton"
        />
</androidx.constraintlayout.widget.ConstraintLayout>