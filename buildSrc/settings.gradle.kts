pluginManagement {
    plugins {
        id("de.fayard.refreshVersions") version "0.10.1"
////                                # available:"0.10.1"
    }
}

plugins {
    id("de.fayard.refreshVersions")
}